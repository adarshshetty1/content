name: Archive Creation
on:
  pull_request:
    branches:
      - master
jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repositoryssss
        uses: actions/checkout@master
        with:
          fetch-depth: 0
      - run: |
          echo `git branch -r`
          git diff $GITHUB_HEAD_REF...$GITHUB_BASE_REF --name-only | grep -E 'dashboards|reports|workbooks' > diff.txt
          cat diff.txt
